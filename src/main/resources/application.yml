spring:
  datasource:
    url: jdbc:h2:mem:brokerdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true
broker:
  stock-exchange-url: http://localhost:5000